<tr>
  <td style="width: 35px;">
    <% unless stage.rest_day? %>
      <% if stage.general_predict? %>
        <span class="badge badge-warning">G</span>
      <% else %>
        <%= '<span class="badge badge-success">'.html_safe if stage.start_at.today? %>
        <%= stage.stage_number %>
        <%= '</span>'.html_safe if stage.start_at.today? %>
      <% end %>
    <% end %>
  </td>
  <td class="col-md-2">
  <% unless stage.rest_day? %>
    <%= l stage.start_at, format: :short %>
  <% else %>
    <%= l stage.start_at, format: '%d %b' %>
  <% end  %>
  </td>
  <td>
  <% unless stage.rest_day? %>
    <% if stage.general_predict? %>
      <%= link_to t('message.general_predict'), race_stage_path(@race, stage) %>
    <% else %>
      <%= stage_type stage %>
      <%= link_to stage.name, race_stage_path(@race, stage) %>
    <% end %>
    <%= predict_status stage %>
  <% else %>
    <%= t 'message.rest_day' %>
  <% end %>
  </td>
  <td class="col-md-2">
    <%= stage.winner if stage.winner %>
  </td>
</tr>
