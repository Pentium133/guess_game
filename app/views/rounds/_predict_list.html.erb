<% pr = match.match_predicts.active.order(:updated_at) %>
<% if pr.count > 0 %>
<table class="table table-condensed table-bordered no-margin">
  <tbody>
    <% pr.each do |predict| %>
      <%= render predict %>
    <% end %>
  </tbody>
</table>
<% end %>
